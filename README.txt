Оновлення: обидві панелі редагуються через CMS

1. Supabase → SQL Editor → запустити two-editable-panels.sql
2. GitHub → замінити:
   index.html
   app.js
   admin.html
   admin.js
3. Vercel → дочекатися Ready
4. CMS:
   • «Для пацієнта» — редагуються заголовок секції, пояснення та всі картки.
   • «Інформація за напрямками» — редагуються малий заголовок, головний заголовок, пояснення та всі картки/посилання.
